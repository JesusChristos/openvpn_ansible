---
CLIENT_CONNECTION_INTERFACE: "CONNECTION_INTERFACE"
CLIENT_CONNECTION_PROTOCOL: "CONNECTION_PROTOCOL"
CLIENT_REMOTE_CONNECTION_SERVER: 
CLIENT_CONNETCTION_PORT: "{{ CONNECTION_PORT }}"
CLIENT_PUSH_EXTRA_CONFIG: "{{ PUSH_EXTRA_CONFIG }}"
CLIENT_PUSH_EXTRA_DNS_1: "{{ PUSH_EXTRA_DNS_1 }}"
CLIENT_PUSH_EXTRA_DNS_2: "{{ OPENVPN_VERB_LEVEL }}"
CLIENT_CHIPER: "{{ CHIPER }}"
CLIENT_OPENVPN_USER: "{{ OPENVPN_USER }}"
CLIENT_OPENVPN_GROUP: "{{ OPENVPN_GROUP }}"
CLIENT_OPENVPN_VERB_LEVEL: "{{ OPENVPN_VERB_LEVEL }}"
USERNAME_DIR: /etc/openvpn/client
CERT_ROOT_DIR: certificate_authority
